package com.devplatform.business.mapper;
import com.devplatform.sys.mapper.BaseMapper;

/**
 * ${bean.className} Mapper
 * 用于${bean.tableRemark}的数据库操作
 * @author Administrator
 *
 */
public interface ${bean.className}Mapper<T> extends BaseMapper<T> {
	
	
}
